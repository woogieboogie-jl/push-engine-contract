# Node.js dependencies
node_modules/

# Foundry dependencies & build artifacts
lib/
cache/
out/
broadcast/

# Environment files (secrets)
.env

# Other lockfiles (to enforce using only one, e.g., pnpm-lock.yaml)
package-lock.json
yarn.lock
